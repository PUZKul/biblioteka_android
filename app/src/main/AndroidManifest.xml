<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kul.pl.biblioteka">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.Login"></activity>
        <activity android:name=".register.Register" />
        <activity android:name=".BookView" />
        <activity android:name=".readingAndHistory.ReadingAndHistory_Main" />
        <activity android:name=".homeView.ListHelper" />
        <activity android:name=".homeView.HomeList" />
        <activity android:name=".BottomNavigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>